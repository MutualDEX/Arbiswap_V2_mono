{
  "name": "other_contracts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.1.0",
    "arb-ethers-web3-bridge": "^0.7.3"
  },
  "scripts":{
    "migrate": "truffle migrate --reset",
    "migrate_local": "npx truffle migrate --reset --network local_arbitrum",
    "migrate_remote": "npx truffle migrate --reset --network remote_arbitrum"

  }
}
